export = () => ({
  //removeIf(debug)
  debug: false,
  // endRemoveIf(debug)

  //removeIf(!debug)
  notDebug: false,
  //endRemoveIf(!debug)

  // removeIf(prod)
  prod: false
  //endRemoveIf(prod)
})
